import { CodeValuePipe } from './code-value.pipe';

describe('CodeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CodeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
